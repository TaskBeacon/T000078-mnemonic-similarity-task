# Task Plot Review

## Round 1

- Evidence match: four rows, phase order, keys, object duration, and ISI duration are correct.
- Layout: no overlap; screen boxes, arrows, row labels, and timing labels are readable.
- Scale: photographs preserve recognizable proportions and fit inside equal-size screens.
- Header-safe area: present and free of generated branding.
- Blocking issue: the LURE screen resembles the studied `100a` spray bottle rather than the required tested `100b` bottle. This is an incorrect stimulus member for the lure condition.

## Round 2 Request

Replace only the object photograph inside the LURE row's first screen with the supplied `100b.jpg` tested lure image. Preserve every other pixel-level layout choice, label, timing, row, arrow, blank screen, and header-safe area.

## Round 2 Final Review

- Evidence match: PASS. Study, target, lure, and foil rows are present once each.
- Stimulus identity: PASS. Study/target use the same cabinet, lure uses the tested `100b` bottle with angled gray trigger head and tapered bottle, and foil uses the unseen lock-pick set.
- Phase order and timing: PASS. Every row shows object (2.0 s) followed by blank ISI (0.5 s); no fixation or feedback was added.
- Response prompts: PASS. Study uses V/N indoor/outdoor; all test rows use V/B/N old/similar/new.
- Layout and scale: PASS. No overlaps, labels are readable, screen boxes are equal, photos are recognizable and undistorted, and whitespace is balanced.
- Branding: PASS after fixed post-processing. Centered title, centered `Construct:` subtitle, and borderless top-right TaskBeacon lockup are present without covering timeline content.
- README embed: PASS.

Final verdict: accepted after two image-generation rounds.
