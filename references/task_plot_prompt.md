Use case: infographic-diagram
Asset type: TaskBeacon task flow diagram
Primary request: Create a clean, publication-ready task flow diagram as a timeline collection for the behavioral task described below.

Task: Mnemonic Similarity Task
Construct: episodic memory / pattern separation / hippocampal function
Rows/conditions:
- STUDY: incidental indoor/outdoor judgment.
- TARGET: exact repetition of a studied object.
- LURE: similar but nonidentical paired object.
- FOIL: previously unseen object.

Timeline phases:
- STUDY: OBJECT (2.0 s; V or N; centered wooden drawer cabinet; prompt `INDOOR OR OUTDOOR?` and `V INDOOR · N OUTDOOR`) -> BLANK ISI (0.5 s; empty white screen).
- TARGET: OBJECT (2.0 s; V, B, or N; the exact same wooden drawer cabinet; prompt `OLD, SIMILAR, OR NEW?` and `V OLD · B SIMILAR · N NEW`) -> BLANK ISI (0.5 s; empty white screen).
- LURE: OBJECT (2.0 s; V, B, or N; the attached tested spray-bottle image that is similar but not identical to its studied pair; prompt `OLD, SIMILAR, OR NEW?` and `V OLD · B SIMILAR · N NEW`) -> BLANK ISI (0.5 s; empty white screen).
- FOIL: OBJECT (2.0 s; V, B, or N; the attached previously unseen lock-pick tool set; prompt `OLD, SIMILAR, OR NEW?` and `V OLD · B SIMILAR · N NEW`) -> BLANK ISI (0.5 s; empty white screen).

Visual requirements:
- White background, landscape orientation, crisp dark text, restrained condition accent colors.
- One horizontal row per condition or representative trial type.
- Each row contains exactly two participant-screen snapshots connected by a subtle arrow.
- Each first screen shows the specified visible photograph and compact prompt; each second screen is blank white.
- Use gray participant-screen boxes, thin black arrows, consistent row spacing, and subtle row separators.
- Place timing labels under each screen in compact text.
- Place condition labels at the left of each row.
- Use short labels only; avoid paragraphs inside the image.
- Make all text legible at normal document preview size.
- Preserve the supplied photographs' aspect ratios and recognizable content.
- Leave a clean blank header band across the top 15-18% of the image. This band is reserved for a fixed title, `Construct: ...` subtitle, and TaskBeacon logo lockup that will be added after generation.

Accuracy constraints:
- Do not invent phases, stimuli, condition names, keys, rewards, or timings.
- Do not add fixation crosses, feedback, correctness marks, scores, people, lab equipment, decorative scenes, logos, or unrelated icons.
- Do not draw the task title, construct subtitle, any logo, watermark, brand mark, or `TaskBeacon` text inside the generated image.
- Draw only the timeline content below the blank header band.
- Preserve these exact terms: STUDY, TARGET, LURE, FOIL, OBJECT, BLANK ISI, 2.0 s, 0.5 s, V, B, N.

Style:
TaskBeacon scientific infographic style: clean vector-like raster image, organized spacing, gray screen boxes, restrained color accents, and a blank header-safe area.
